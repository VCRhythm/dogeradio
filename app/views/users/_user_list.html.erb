<% if users.length > 0 %>
	<% users.each do |user| %>
		<%= link_to user, remote: true, class: "remote-link" do %><%= image_tag user.avatar(:thumb), data:{toggle:"tooltip", placement:"bottom"} %><% end %>
	<% end %>
<% else %>
	<p>No users found.</p>
<% end %>