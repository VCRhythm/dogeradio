<% if track.user.try(:avatar) %>
	<%= link_to track.user, remote: true, class: "remote-link" do %><%= image_tag track.user.avatar.url(:thumb), id: "player-image", class: "media-object" %><% end %>
<% end %>
<span id="thanks" style="position:relative; margin-bottom:0; margin-top:-50px;"></span>
