<% vote ||= false %>
<% track.tags.each do |tag| %>
	<% if vote==false %>
		<%= render partial: 'tags/tag', locals: {tag: tag} %>
	<% else %>
		<%= render partial: "tags/tag_vote", locals: {tag: tag} %>
	<% end %>
<% end %>
